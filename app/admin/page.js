export const dynamic = "force-dynamic";
import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
import Link from "next/link";
export default async function Admin(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const {data:profile}=await supabase.from("profiles").select("*").eq("id",user.id).single();if(!["owner","enterprise"].includes(profile?.role))redirect("/dashboard");const {data:apis}=await supabase.from("api_catalog").select("*").order("created_at",{ascending:false});const {count}=await supabase.from("profiles").select("*",{count:"exact",head:true});return <main className="mx-auto max-w-7xl px-5 py-10"><div className="flex items-center justify-between"><div><h1 className="text-4xl font-black">Admin Panel</h1><p className="text-zinc-500">Manage API catalog, users and platform configuration.</p></div><Link href="/dashboard" className="rounded-xl border border-white/10 px-4 py-2">Dashboard</Link></div><div className="mt-7 grid gap-4 md:grid-cols-3"><div className="glass rounded-2xl p-5"><span className="text-zinc-500">Users</span><div className="mt-2 text-3xl font-black">{count||0}</div></div><div className="glass rounded-2xl p-5"><span className="text-zinc-500">Published APIs</span><div className="mt-2 text-3xl font-black">{apis?.length||0}</div></div><div className="glass rounded-2xl p-5"><span className="text-zinc-500">Your role</span><div className="mt-2 text-3xl font-black uppercase">{profile.role}</div></div></div><div className="mt-7"><Link href="/admin/apis" className="glass inline-block rounded-2xl p-6 hover:border-violet-400/30"><div className="text-xl font-bold">API Manager</div><p className="mt-2 text-zinc-500">Tambah/edit/publish endpoint dari panel admin.</p></Link></div></main>}

export const dynamic = "force-dynamic";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import DashboardShell from "@/components/DashboardShell";
export default async function Page(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect("/login");const {data:profile}=await supabase.from("profiles").select("*").eq("id",user.id).single();return <DashboardShell profile={profile}><div className="space-y-5"><div><h1 className="text-3xl font-black">Dashboard</h1></div><div className="grid gap-4 md:grid-cols-4"><div className="glass rounded-2xl p-5"><div className="text-zinc-500">Role</div><div className="mt-2 text-xl font-bold uppercase">{profile?.role}</div></div><div className="glass rounded-2xl p-5"><div className="text-zinc-500">Plan</div><div className="mt-2 text-xl font-bold uppercase">{profile?.plan}</div></div><div className="glass rounded-2xl p-5"><div className="text-zinc-500">Daily Limit</div><div className="mt-2 text-xl font-bold">{profile?.daily_limit?.toLocaleString?.() || "100"}</div></div><div className="glass rounded-2xl p-5"><div className="text-zinc-500">Status</div><div className="mt-2 text-xl font-bold text-emerald-400">ACTIVE</div></div></div><div className="glass rounded-2xl p-6"><h2 className="font-bold">Quick start</h2><p className="mt-2 text-zinc-400">Buat API key, buka dokumentasi, lalu panggil endpoint /api/v1/*.</p></div></div></DashboardShell>}
